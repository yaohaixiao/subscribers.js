section.section
  header.section__header
    h3#method-deleteSubscriber.section__h3 deleteSubscriber(topic)
  div.section__content
    h4.section__h4 Description
    p 删除特定 topic 主题的订阅者信息。
    h4.section__h4 Parameters
    h5.section__h4 topic
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd String
    p （必须）主题名称。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Object。
    p Subscribers 对象。
    pre.section__pre
      code.section__code.
        Subscribers.subscribe('author', handlerAuthor)

        // 删除 author 主题相关的所有信息
        Subscribers.deleteSubscriber('author')
