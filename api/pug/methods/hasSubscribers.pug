section.section
  header.section__header
    h3#method-hasSubscribers.section__h3 hasSubscribers(topic)
  div.section__content
    h4.section__h4 Description
    p 判断是否存在包含 topic 指定的订阅者信息。
    h4.section__h4 Parameters
    h5.section__h4 topic
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd String
    p （必须）主题名称。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    pre.section__pre
      code.section__code.
        Subscribers.subscribe('author', handlerAuthor)
        Subscribers.subscribe('author.career.years', handlerYears)

        Subscribers.hasSubscribers('author.career.years')
        // => true

        Subscribers.hasSubscribers('author.career.year')
        // => true 因为包含 author 和 author.career 主题
